package com.github.mustahsen.broadcaster.enums;

/**
 * The enum Broadcast value source.
 */
public enum BroadcastValueSource {
    /**
     * Argument broadcast value source.
     */
    ARGUMENT,
    /**
     * Constant broadcast value source.
     */
    CONSTANT,
    /**
     * Return value broadcast value source.
     */
    RETURN_VALUE,
    /**
     * Object broadcast value source.
     */
    OBJECT
}
